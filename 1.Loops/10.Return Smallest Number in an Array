#Return a smallest number from an array

Example:
Numbers = [10,25,65,2,35,1]
Output = 1

Logic : 
1) Just Assume 1st element is smallest element
2) Go to each element in the array and ask
   - Are u an number smaller than smalles element
   - if say yes --> update the current number as smallest element
   - if no --> continue to check and don't update the smallest element


Numbers = [10,25,-3,9,2,3,1,-7]

#Find no.of elements in the array
size = len(Numbers)

if size == 0:
    print("Array is empty")
elif size ==1:
    print("The Smallest Number is", Number[0])
else:
    #Assuming small number is 1st number
    small_number = Numbers[0]
    for i in range(1,size):
        if(small_number > Numbers[i]):
            small_number = Numbers[i]
    print("The Smallest Number is ",small_number)


